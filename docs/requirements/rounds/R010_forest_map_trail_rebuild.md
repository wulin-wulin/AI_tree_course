# R010 - 知识森林地图重构为「沿途生长的徒步长卷」

状态：Implemented

创建日期：2026-06-01

确认日期：2026-06-01

## 背景

R005–R008 把章节地图迭代成「8 棵章节树 + 蛇形小径 + 点击弹章节预览方框」的俯览扁平地图。需求讨论窗口在本轮开场对现状做了实测（桌面 1280×820 / 移动 390 宽截图）并确认了几个核心问题：

- **版面两头不讨好**：固定 1000×700 比例等比缩放，桌面首屏放不下整幅地图（第 7、8 章与「林深处」落在折叠线外），移动端地图被压成 ~259px 高的小条、树与下一行标题竖向打架，且地图下方留约 340px 空白，整体偏 demo。
- **构图右下偏空**、背景装饰树规整呆板像占位符、整幅偏「干净示意图」缺森林氛围。
- 知识点（小节）只藏在点击后的预览方框里，地图本身信息量低。

用户在本轮选择「森林地图继续打磨」方向并明确「允许调整版面」，随后给出更进一步的创意诉求（见下）。本轮据此对章节地图（Level 1，路由 `/ai`）做一次结构性重构。

## 用户真实反馈

> 原话保留：

- 「我想要让路更长一些，然后将每章的小节也放在地图上，章节之间用不同颜色的背景和不同的树进行表示。」
- 「方式2（纵向长卷向下滚动），然后需要分布均匀，不能太密集也不能太稀疏。」
- 「不要太窄，一行不能仅放两个小节，需要有蜿蜒曲折的感觉。」
- 「路需要更加圆滑一些，你需要选择一下针对这个的图形学算法。」

本轮设计经「需求讨论窗口」用浏览器可视化草图（superpowers brainstorming 可视化陪同）逐项确认：地图整体结构、滚动方向、小节节点样式、点击交互模型、桌面/移动响应式、曲线算法，均由用户在草图上逐一拍板。

## 本轮目标

把 Level 1 章节地图从「8 棵树 + 预览方框」重构为**一条贯穿全部 49 个小节、宽幅蜿蜒、向下滚动的徒步长卷**：路更长、小节直接呈现在地图上、章节以不同生态色区 + 不同树种区分，并顺带解决旧版的视口适配、构图留白与森林氛围问题。

## 具体需求

### 1. 结构与叙事
- 地图主体改为**一条连续长路**，按全课程连续顺序（章序 + 章内序）依次串联**全部 49 个知识点（小节）**。
- 顶部「林口」起步、底部「林深处」收尾；**整幅纵向滚动**，自上而下走完旅程。
- 复用现有数据：章节顺序取 `chapters`，章内小节取 `chapterPoints(chapter.id)`；跳转复用 `pointPath` / `findPoint`；进度复用 `readLastPoint`。不新增 / 不改课程数据结构。

### 2. 版面与响应式（核心约束）
> 修订（R010.1）：路由「规则蛇形折返」改为**先随机生成一条有机蜿蜒路、再沿路放树**（见下「修订记录」）。
- 路**大幅度横向蜿蜒、铺满主区宽度**；每一段「波浪」上排 **4–5 棵树**。**禁止**退化成「窄列里左右各一、每行仅 2 点」或「每行形状完全一样的规则蛇形」。
- 树节点**步距均匀**（沿路等弧长采样）——疏密一致，不太密也不太稀疏。
- 桌面：路占满主内容宽度；两侧多余留白用**稀疏装饰森林 + 草地纵深**填满，不再空旷。
- 移动：同一条路波浪幅度收窄、保留同样的曲折感，**每行仍多于两个小节**；不再出现旧版「又挤又空」。
- 桌面与移动是同一条长路的不同尺度（不再是「固定比例整幅缩放」那种两头不讨好的方案）。

### 3. 章节表示：小路分段染色 + 不同树种
> 修订（R010.1）：原「淡色生态色区椭圆」被否（很丑），改为**小路按章节分段染色**——不再有背景色块。
- 8 个章节 = **小路按章节分段着色**：每章覆盖其节点弧长区间的路段用该章 `accent`（半透明覆盖在土色底带上）；不使用背景椭圆/色块。
- 每章开头一棵明显更大的**地标树**，**8 章用 8 种不同树种剪影**，沿用现有扁平几何风格。建议初始映射（执行可微调，但须 8 种可清晰区分）：
  1. 01 导论与历史 — 层叠针叶松（teal）
  2. 02 搜索与问题求解 — 高瘦白杨（orange）
  3. 03 知识表示与推理 — 圆冠阔叶树（purple）
  4. 04 机器学习基础 — 宽冠橡树（blue）
  5. 05 神经网络与深度学习 — 柱形塔柏（pink）
  6. 06 自然语言处理与计算机视觉 — 细干桦树（teal-green）
  7. 07 强化学习与智能体 — 垂枝柳树（olive）
  8. 08 生成式 AI 与安全伦理 — 团簇矮灌丛（coral）
- 视觉层级：**地标树明显更大、树种各异；小节树苗更小、同章同色统一**，避免 49 棵小树与 8 棵地标树互相抢视觉。

### 4. 小节表示与交互（交互模型 1）
- 49 个小节 = 该章颜色的**小树苗**，沿路生长；**标题常驻**：
  - 桌面：标题在树苗旁、沿路左右交替排布，避免互相遮挡。
  - 移动：标题置于树苗**下方**、更紧凑（防止窄屏横向碰撞）。
- **点击直达**：
  - 点小节树苗 → 进入**该小节阅读页**（`pointPath`）。
  - 点章节地标树 → 进入**该章第一个小节**的阅读页。
- **悬停 / 键盘聚焦**小节或地标树 → 浮出**一张轻预览卡**（标题 + 一句梗概 / 引子）；**任意时刻最多一张**预览卡可见（延续 R009 「单预览卡」原则，鼠标移开 / 切换焦点即消失，不残留）。
- **移除 R008 章节预览大方框**：`ChapterPreviewDialog` 退役（小节已直接在地图上，方框冗余）。

### 5. 进度与状态（按小节粒度）
- 进度粒度由「按章」改为**按小节**：
  - 已读过的小节树苗 = **「已长成」**（饱满色）。判定口径：全程连续序列中，**位于「上次读到点」及其之前**的所有小节视作已长成。
  - 其余（位于上次读到点之后）= **「未长成」**（淡色）。
  - 「上次读到点」那棵 = 高亮标注 **「上次读到这里」**。
  - 无任何阅读记录时：全部为「未长成」，不显示「上次读到这里」。
- 进度来源沿用 localStorage（`readLastPoint`）。

### 6. 曲线算法（路要圆滑）
- 小径改用**向心 Catmull-Rom 样条（centripetal，α = 0.5）**，逐段转三次贝塞尔，**替换现有均匀（uniform）版 `buildTrail`**。
- 硬约束：曲线必须**精确穿过每个节点**（树苗须落在路上），故只能用插值型样条；向心参数化可避免均匀版在间距不均 / 急转弯处的过冲、尖点与自交。
- 参考实现（Barry-Goldman 非均匀切线；`alpha=0.5` 即向心，端点用首尾点复制）：

  ```
  对相邻四点 p0,p1,p2,p3：
    d01=|p1-p0|, d12=|p2-p1|, d23=|p3-p2|
    t0=0, t1=t0+d01^α, t2=t1+d12^α, t3=t2+d23^α      // α=0.5
    m1 = (p1-p0)/(t1-t0) - (p2-p0)/(t2-t0) + (p2-p1)/(t2-t1)
    m2 = (p2-p1)/(t2-t1) - (p3-p1)/(t3-t1) + (p3-p2)/(t3-t2)
    段 p1→p2 的贝塞尔控制点：
      b0=p1
      b1=p1 + (t2-t1)/3 · m1
      b2=p2 - (t2-t1)/3 · m2
      b3=p2
  ```
  （需对各分母做零保护；起点 p0:=p1、终点 p3:=p2。）

### 7. 氛围与背景
- 草地做出纵深、装饰背景树疏密有致，去掉「干净示意图」感，更像一片真森林；装饰树为纯视觉、`aria-hidden`、不承载章节信息、避开路径与节点。

### 8. 顶部与导航
- 保留「返回书架」入口；滚动时做成**纤细常驻顶栏**，含轻量进度指示（如「走到第 N / 49 棵」或细进度条）。

### 9. 可达性
- 保留语义化等价导航：用有序列表（`<ol>`）按全程顺序承载 49 个小节，**键盘可按顺序 Tab**、聚焦时显示该节预览卡。
- 保留可见焦点环与必要 ARIA；点击区域为真实可聚焦控件（`<button>` / `<a>`）。
- 尊重 `prefers-reduced-motion`：滚动/悬浮动效在该偏好下降级。

## 非目标

- 不改 Level 2 阅读页的内容分层与交互；不改首页书架（Level 0）。
- 不改课程数据结构与路由（HashRouter 三级结构不动）。
- 不引入新依赖、不接入后端 / 外部图片。
- 不在本轮新增自测、概念关联探索等导航以外的学习交互（留待后续轮次）。
- 不重做 R008 方框逻辑本身——而是**整体移除**它（`ChapterPreviewDialog` 退役）。

## 验收标准

> 验收方式沿用：人类用户**主观评审** + 截图自检。

- 地图为**一条连续长路**，按全课程顺序串起**全部 49 个小节**；自上而下纵向滚动可走完「林口 → 林深处」。
- 路**宽幅蜿蜒铺满宽度**，每段波浪 3–5 个小节；**没有**退化成每行仅 2 点的窄列形态；节点**步距均匀**。
- 桌面首屏不再裁切错位、两侧不空旷；移动端不再「又挤又空」，每行仍多于两个小节、标题不互相遮挡。
- 8 章各有**可区分的生态色区 + 不同树种地标**；小节树苗更小、同章同色，不抢地标视觉。
- 点小节 → 对应阅读页；点章节地标树 → 该章首点；悬停 / 聚焦浮出预览卡且**任意时刻最多一张**；旧的章节预览大方框已移除。
- 进度按小节生效：已读「已长成」、未读「未长成」、上次读到的高亮「上次读到这里」。
- 小径曲线肉眼圆滑、无明显过冲 / 打结，且每个树苗都落在路上（向心 Catmull-Rom）。
- 键盘可按顺序 Tab 过全部小节并触发预览；焦点环可见；`prefers-reduced-motion` 下动效降级。
- `npm run build`（`tsc -b && vite build`）通过。
- 按 `AGENT_PROTOCOL.md` 第 7 步**自检视觉**：Playwright 截 `/#/ai` 桌面 + 移动并滚动走查，自己核对达标后再报告完成。

## 执行备注

- 改动集中在 `src/components/ChapterMapPage.tsx` 与 `src/styles.css`；**退役** `src/components/ChapterPreviewDialog.tsx`（移除引用，组件文件可删除或停用）。
- `buildTrail` 升级为向心 Catmull-Rom（见「具体需求 6」公式）；节点坐标布局可由「波浪行 + 每行 N 个 + 章节分段」生成，注意保证步距均匀与铺满宽度。
- 长卷为可滚动内容：注意页面/容器的滚动与高度处理（旧版本机预览里曾出现滚动不顺，重构时确保正常纵向滚动、顶栏 sticky 不挡内容）。
- 与 R009 的关系：R009 修的是**旧方框流程**里的「双预览卡」bug；本轮整体移除该方框，但**新悬停预览卡仍须保持「任意时刻最多一张」**这一不变量。R009 自身的正式验收独立处理（本轮不改其状态、不改 `current_state.md`）。
- 范围最小化但允许版面重排（本轮已确认「允许调整版面」）；不做与地图无关的重构。

## 执行反馈

> 本轮由需求讨论窗口在用户明确「你实现吧」后直接执行（用户为项目控制者，其指令优先于讨论/执行分窗口的流程约定）。

执行日期：2026-06-01

### 实现概况

把 Level 1 章节地图从「8 棵树 + 蛇形小径 + 点击弹方框」整体重构为**一条贯穿全部 49 个知识点的纵向长卷徒步道**：一条蜿蜒长路按全课程顺序串起 8 个章节地标 + 49 个小节树苗（共 57 个节点），自上而下滚动从「林口」走到「林深处」。版面随容器宽度自适应（宽屏每行 5 个、波浪更宽；窄屏每行 3 个、行数更多 → 更长滚动），小径改用向心 Catmull-Rom 样条使曲线圆滑，章节以生态色区 + 8 种不同树种区分，移除了旧的章节预览方框。

### 已完成需求

- **长路串起全部小节**：`orderedPoints`（49）按章序 + 章内序，前置各章地标，生成 57 个节点的旅程序列，一条连续小径依次穿过。顶「林口」、底「林深处」，纵向滚动走完。
- **宽幅蜿蜒、铺满宽度、每行多于两个**：`computeLayout` 按容器宽度选 `perRow`（≥860→5、≥600→4、否则 3），蛇形折返 + 每行正弦拱起（`bowAmp` 桌面 52 / 移动 34）形成明显蜿蜒；节点等步距铺满可用宽度。实测桌面每行 5、移动每行 3，均 >2。
- **响应式**：用 `ResizeObserver` 测量场景宽度按宽度重算版面。桌面长卷约 2.5k px、移动约 3.4k px，皆正常纵向滚动；移动端不再「又挤又空」，桌面不再裁切。
- **章节色区 + 不同树种**：每章一块 `soft` 色生态椭圆色区（沿路有机铺底）；8 种扁平几何树种剪影（松/杨/圆冠/橡/塔柏/桦/柳/灌丛）与 8 章一一对应；地标树更大且带章节号，小节树苗更小、同章同色。
- **交互模型 1**：点小节树苗 → 该点阅读页（实测点「图灵测试」跳到 `#/ai/intro-history/turing-test`）；点章节地标 → 该章首点；悬停/聚焦浮出**单张**轻预览卡（eyebrow + 标题 + `shortSummary`），`previewKey` 单状态保证任意时刻最多一张（实测 `previewedCount=1`）；**已移除** `ChapterPreviewDialog`。
- **按小节进度**：上次读到点及之前的小节/已抵达章节地标为「已长成」（饱满 accent + dark 单侧明暗），其后为「未长成」（淡色），上次读到点标「上次读到这里」；顶栏显示「已点亮 N/49」+进度条。实测 lastPoint=knowledge-graph 时 17/49、20 节点饱满、current 标记正确。
- **向心 Catmull-Rom（α=0.5）**：`buildTrail` 用 Barry-Goldman 非均匀切线逐段转贝塞尔，曲线穿过每个节点、无过冲。
- **氛围与顶栏**：草地纵向渐变 + 两侧稀疏装饰背景树填充留白；纤细 sticky 顶栏含返回书架 + 进度（窄屏隐藏中间冗余标题、各元素不换行）。
- **可达性**：节点为 `<ol>` 内 `<button>`，DOM 顺序＝旅程顺序，键盘可顺序 Tab；聚焦显示预览卡 + 可见焦点环；`prefers-reduced-motion` 下过渡降级。

### 未完成或部分完成

- 无。所有「具体需求」项均已实现并自检。

### 执行中发现的问题

- 旧 `:focus` vs 程序化焦点的 R009 类问题在新设计中天然消失：新版无方框、无关闭后程序化 `focus()` 还原，预览卡由单一 `previewKey` 状态驱动，结构上保证单卡。
- 初版每行偏平直（蛇形折返但行内是直线），蜿蜒感不足；将每行正弦拱幅 `bowAmp` 从 30/20 提升到 52/34 后明显改善，且因中列节点上下错开反而不影响节点间距。
- 移动端顶栏初版「返回书架/知识森林地图」会换行；改为返回链接 `white-space:nowrap`、窄屏隐藏中间标题、`flex-shrink:0` 后单行不溢出。

### 认为需求不合理或需要澄清的点

- 8 种树种为初版剪影，区分度已足够但仍可继续美化（如柳树下垂枝、桦树多叉的精细度）；若后续要更写实可再单独打磨，不属本轮阻塞项。
- 色区为「每章节点簇外接椭圆」，因一章节点常横跨整行，色区偏「横向色带」而非局部色块；视觉上读作「一条带＝一章」清晰，如更想要局部生态斑块可后续调整。

### 修改文件清单

- `src/components/ChapterMapPage.tsx`：整体重写（向心 Catmull-Rom `buildTrail`、`computeLayout` 响应式蜿蜒布局、`ResizeObserver` 测宽、57 节点旅程序列、单预览卡状态、按点进度、sticky 顶栏、8 种 `TreeArt` 树种）。
- `src/components/ChapterPreviewDialog.tsx`：删除（方框退役）。
- `src/styles.css`：移除旧 `.forest-scene/.forest-tree/.tree-card/.chapter-preview*` 等约 560 行；新增 R010 长卷样式（顶栏、色区、树苗/地标、树形状态着色、预览卡、装饰森林、响应式、reduced-motion）。
- `docs/next_upgrade_prompt.md`：当前执行轮次指向 R010。
- `docs/requirements/rounds/R010_forest_map_trail_rebuild.md`：本文档（状态 Ready + 执行反馈）。
- `.gitignore`：新增忽略 `.superpowers/`（需求讨论的可视化草图目录，brainstorming 可视化陪同产物）。

### 验证结果

- `npm run build`（`tsc -b && vite build`）通过，无类型错误（1586 模块，产物输出 `dist/`）。
- 视觉自检（Playwright 预览 `/#/ai`，桌面 1180 宽 + 移动 390 宽 + 整幅长卷）：
  - 节点 57（8 地标 + 49 小节），桌面每行 5 / 移动每行 3，蜿蜒曲折、步距均匀、铺满宽度；标题无重叠（实测 0 处）。
  - 8 章色区与树种分明；地标大、小节小。
  - 进度态：饱满/淡色对比明显、「上次读到这里」+ 顶栏「已点亮 N/49」正确。
  - 悬停/聚焦单张预览卡（含 `shortSummary`），`previewedCount=1`。
  - 点击小节跳对应阅读页（`#/ai/intro-history/turing-test`）；控制台无报错。
  - 滚动从「林口」到「林深处」全程正常。

### 修订记录（R010.1 · 用户迭代反馈）

用户看过首版后提出三点，已全部实现并自检：

1. **「路在重复」→ 先随机生成路、再沿路放树**：不再用规则蛇形折返。改为 `mulberry32` 确定性随机生成锚点（交替贴近左缘 `[0.10,0.15]W` / 右缘 `[0.85,0.90]W`，相邻**横向跨度落在 0.70–0.80 倍地图宽**，竖向步高随机），向心 Catmull-Rom 平滑成一条始终向下、不重复的有机蜿蜒路；再对该路**等弧长采样**放置 57 个节点。固定种子 → 路形稳定不乱跳。「实例 A」（种子 7）为用户选定风格。
2. **树太小 → 放大、每段 4–5 棵**：`landmarkSize` 62→78（移动 48→60）、`pointSize` 38→52（移动 30→42）；`treesPerSweep` 桌面 5 / 移动 4；并相应加大每段竖向落差 `stepBase`（桌面 190 / 移动 182）避免大树压到邻段标题。
3. **椭圆色区很丑 → 去掉，改小路分段染色**：移除区域椭圆；改为按章节弧长区间给路段染 `accent`（半透明覆盖在土色底带上），背景恢复纯净草地。

新增/调整：`mulberry32` / `crSegments` / `flatten` / `sampleNodes` / `rangeD` 等几何函数；`computeLayout` 改为有机路径生成 + 等弧长放树 + 分段染色 + 稀疏装饰树；CSS 用 `.trail-seg` 替换 `.trail-region`，虚线改浅色衬在彩色路上；顶/底留白加大以容纳大地标树与「林口/林深处」标签。修订后重新 `npm run build` 通过；桌面 + 移动自检蜿蜒不重复、树明显放大、路段分章染色、标题 0 处重叠、林口/林深处与进度态正常。

### 修订记录（R010.2 · 用户第二轮反馈）

用户基于 R010.1 又提出四点，已全部处理：

1. **地图没居中**：`.forest-trail-page` 缺 `margin:0 auto`，宽屏左对齐。已补；实测 1440 视口下场景左右边距相等（各 140px）。
2. **树至少放大到 2 倍**：`landmarkSize` 78→150、`pointSize` 52→104（约 2×；窄屏因放不下大树取 108 / 76 ≈1.8×并相应加大 `stepBase`）；标题字号同步放大。
3. **路段配色边界重叠变深**：原半透明 + 圆头描边在边界处两段叠加变深。改为**不透明 + 平头（`butt`）描边 + 用精确插值点在相邻章节弧长中点处对接**（新增 `interpAt` / `segPath` 取代 `sampleNodes` / `rangeD`），边界干净、无叠色。
4. **每个转弯只放一棵、每条斜线 3–4 棵**：放置规则改为「每个锚点（转弯）正好 1 棵 + 斜线内按弧长均匀铺 `m` 棵」，并在每段两端各留约 1.5 倍树宽净空，使转弯处的单棵树有空当；桌面每斜线 3–4 棵（`anchorCount=13`）、窄屏约 3 棵（大树物理可放下）。

复验：`npm run build` 通过；桌面（1440/1280）整页居中、树 2×、绿→橙→紫…路段染色边界干净、转弯单树、标题 0 处重叠、点击跳转与进度态正常。已知小瑕疵：移动端（390 宽）某章节地标长标题与相邻节点标题有 1 处轻微相触（桌面无），属窄屏 + 大树的边界情况，后续如需可再微调种子/字号。

### 修订记录（R010.3 · 用户第三轮反馈）

用户决定**放弃路径随机性**，改两点：

1. **去随机、改规则之字形**：路改为确定性之字形——锚点交替落在左缘 `0.13W` / 右缘 `0.87W`（窄屏内收为 `0.2W`/`0.8W` 防大树顶边），竖向等步高；用**直线段折线**（圆角接头）成路，不再用随机锚点/平滑大波浪。`crSegments`/`flatten`/`bez` 因此弃用（被 tree-shake）。
2. **树分列路的两侧**：树不再落在路上，而是沿路的**法线方向 ±OFFSET 交替偏到两侧**（桌面 96 / 窄屏 64），形成「林荫道」；路居中保持干净。新增 `pointNormalAt`（取路上点 + 单位法线）。首棵向内偏以给顶部「林口」留空；「林口/林深处」改挂到路端点 + 场景顶/底。
3. 斜线每边树数沿用上轮 3–4 棵（`anchorCount=13`，窄屏 14）。

复验：`npm run build` 通过；桌面 1280/1440 居中、规则之字形、树分列两侧的林荫道、路段分章染色边界干净、标题 0 处重叠。移动端（390）剩 2 处轻微标题相触（桌面无），属窄屏 + 2× 大树边界情况。

### 修订记录（R010.4 · 用户第四轮反馈）

用户澄清「之字形」应为**矩形蛇形**（不是斜线）：直线段要**水平**（与上下平行）、转弯是**直角**。据此把布局从「斜线锚点」改写为「水平行 + 直角竖直转弯」：

1. **矩形蛇形路**：按 `perRow`（桌面 5 / 窄屏 4）把 57 个节点分到水平行；偶数行左→右、奇数行右→左，行末直角竖直连到下一行；最后一行只画到最后一棵树。圆角接头。道路 `d` 由这些直角折线生成；章节分段染色按各 item 在路上的解析弧长 `baseArc = r*(行长+行距) + 行内推进` 取中点边界，仍用 `segPath` 平头对接、不重叠。
2. **树分列上 / 下两侧**：树不落在路上，而是沿每个水平行**交替偏到行中线的上方 / 下方**（`offset` 桌面 60 / 窄屏 44）。为避免「朝上的树标题压住下方路面」，新增 `node-above` 类用 CSS `order` 把朝上树的序号/标题**排到树上方**（标题一律朝外）。行距 `rowH` 取 356 / 320 以容纳行间两排朝外标题。
3. 弃用并删除上几版的 `crSegments` / `flatten` / `bez` / `pointNormalAt` 与 `Seg` 类型（矩形蛇形不再需要曲线样条）。

复验：`npm run build` 通过；桌面（1280/1440/960）矩形蛇形、水平直线 + 直角转弯、树分列上下两侧、标题朝外不压路、按章染色、居中；标题重叠 桌面 1 处（换行边缘极轻微）/ 移动 0 处。点击跳转、进度态、林口/林深处正常。

### 修订记录（R010.5 · 用户第五轮反馈）

用户基于矩形蛇形再提三点（附截图），已实现：

1. **转弯加圆弧**：新增 `roundedSerpentine`，把直角角点用二次贝塞尔倒角（半径 `cornerR=30`）再拍平，转弯不再是生硬直角。
2. **连接段（左右竖直段）不放树**：树在每行内**内缩** `inset`（桌面 104 / 窄屏 76），道路仍整行铺满到 `leftX/rightX` 并在端点圆角转弯下行，于是圆角与竖直连接段上不再落树。
3. **树根贴路沿、不在路面上**：树改为按「树干根部」对齐——`.trail-slot` 用 `translate(-50%,-100%)` 让树底（树根）落在锚点；树根 y 取 `rowY ∓ (roadHalf + rootGap)`（路面半宽 12 + 缝 9），即贴着路的上 / 下沿外侧、不压路面。标签改为绝对定位的 `.node-label`，下侧树排树下、上侧树排树上（一律朝外）；预览卡放到标签反侧。
   染色定位改用 `arcOfPoint`（把每个 item 的路上基点投影到圆角路求弧长）。

复验：`npm run build` 通过、无控制台报错；桌面圆角转弯、树根贴路沿（不在路上）、连接段无树、按章染色、居中。标题重叠 3 处（均在章节交界、地标长标题处，轻微）。

补充修正（同轮）：用户指出下侧树「树根还在路上」「树根要落在椭圆阴影正中」。原因是按「图片底边」对齐、而阴影中心在底边上方，落地点被顶到路上。改为**按阴影中心（落地点）锚定**：阴影移到树干根部（SVG `cy=74`），`.trail-slot` 用 `translate(-50%,-88%)` 让阴影中心正好落在锚点；锚点（落地点）取路沿外侧（`roadHalf 12 + rootGap 13`）。效果：树根落在阴影正中、且明显在路面之外，上下侧树各自落在路的上 / 下沿外，路面干净。

## 审核记录

> 由需求讨论窗口审核后填写。
